# --- Core ---------------------------------------------------------------
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0

# --- HTTP / Networking ----------------------------------------------------
httpx>=0.27,<1.0
tenacity>=8.2,<9.0          # retry/backoff for free-tier rate limits

# --- AI Engine clients ------------------------------------------------------
groq>=0.9,<1.0
google-generativeai>=0.7,<1.0

# --- Presentation layer (Phase 5) -------------------------------------------
streamlit>=1.35,<2.0
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0

# --- Utilities ---------------------------------------------------------------
loguru>=0.7,<1.0

# --- Dev / Testing -------------------------------------------------------------
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
mypy>=1.10,<2.0
ruff>=0.4,<1.0
respx>=0.21,<1.0            # httpx mocking for tests
